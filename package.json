{
  "name": "technovit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"next dev\" \"cd admin && sanity start\"",
    "build": "echo 'Building Sanity to public/admin...' && cd admin && sanity build ../public/admin -y && cd .. && cross-env NODE_ENV=production next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "12.2.3",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-loader-spinner": "^5.1.7-beta.1",
    "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "eslint": "8.21.0",
    "eslint-config-next": "12.2.3",
    "@sanity/cli": "^2.30.3",
    "concurrently": "^7.3.0"
  }
}
